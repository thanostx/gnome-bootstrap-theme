# GNOME Deneb
Bootstrap theme for GNOME websites

## Get started
Requirements:
NPM package

#### Running the project:
* Clone the repo
 ```
 git clone https://github.com/thanostx/gnome-bootstrap-theme.git
 ```
 * Install NPM dependencies
 
 ```
 npm install
 ```
  * Run the project
 
 ```
 npm start
 ```
 Visit local webserver http://localhost:8000/ to see or edit the created theme.
